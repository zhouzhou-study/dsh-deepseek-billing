# 更新日志

本项目的所有重要变更都记录在这里。版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

---

## [0.3.0] — 2026-09-13

### 新功能：价格设置面板（自定义价格）

计费面板新增「价格设置」页，可以在界面上维护自己的价格表，**不用改代码**：

- 按模型（或 `*` 表示所有模型）填 **高峰价 / 闲时价** 与 **生效时间**；单价为 输入（缓存未命中）/ 缓存命中 / 输出，单位 元/百万 tokens。
- 数据存在 `$DSH_HOME/billing-prices.json`（可用环境变量 `DSH_BILLING_PRICING_FILE` 覆盖；未设置 `DSH_HOME` 时回退到 `~/.dsh`）。
- **只影响 CNY**：命中自定义条目时 CNY 单价用你填的值，USD 仍沿用官方同刻价。
- **只对生效时间之后生效**：改价不改写更早的账，历史消息仍按当时的政策计价、账单可复现。
- 官方价表（`pricing.js`）保持不变，作为兜底；删除自定义条目即回退官方价。
- 面板分页：「当前生效价」/「我的调价记录」/「官方历史价（折叠）」。
- 新增两个本地只读接口：`GET|POST /api/dsh-deepseek-billing/pricing`（读写自定义价）、`GET /api/dsh-deepseek-billing/usage-by-policy`（按价格档位统计历史用量，后台扫描 + 10 分钟缓存，不阻塞请求）。
- 写盘为**原子写**（先写 `.tmp` 再改名）；文件损坏、条目不合法只会被拒绝或跳过并返回 `errors`，**不会让计费接口 500**。

### 修复：推理 token 不再重复计费

官方的 `completion_tokens` 已经包含 `reasoning_tokens`（DSH 上报的 `outputTokens` 即 `completion_tokens`），此前又按输出价加了一遍，导致“想得多”的会话费用**虚高 40% 以上**。现已修正：reasoning token 仍在明细里展示（它是输出的构成），但不再单独计价。

### 兼容性

- 仍同时支持 DSH **0.1.5+**（`stat`/`open` seam）与 **0.1.0-rc.6**（`readRaw`）两代会话日志接口。
- 新增的价格设置只依赖 Node 内置模块与插件自己的本地路由，不依赖新版 DSH 专有接口，旧版 DSH 上同样可用。

### 校验

- `node --check` 通过（`lib/index.js`、`lib/client.js`、`lib/pricing.js`、`lib/user-pricing.js`、`scripts/install.js`）。
- 价格回归测试 **8/8 通过**（周末闲时 / 工作日峰谷 / 新旧模型名同价 / v4-pro 沿用旧价 / 09-10 调价分界）。
- 价格设置测试 **11/11 通过**：env 覆盖、无 `DSH_HOME` 时回退 `~/.dsh`、源码内**无任何写死的绝对路径**、自定义价在峰/谷两档生效、只影响指定模型、其余模型仍走官方价、删除后回退、坏 JSON 不抛错。

---

## [0.2.2] — 2026-09-13

### 兼容性修复：适配 DSH 0.1.5（会话日志接口换代）

DSH 0.1.5 起，`sessionPersistence` 换成了新的 seam 接口——`stat(id)` 取修订号、`open(id, 'read')` 拿句柄、`handle.read()` 返回**已解析的事件**；旧的 `readStoredRevision` / `readRaw` 已被移除。0.2.1 及更早版本只认旧接口，于是会在新版 DSH 上**静默降级**：余额与当轮费用照常显示，但**「重启前的历史费用回放」失效**（不崩溃，只是少算）。

本次改为**双代接口自动识别**：

| 运行环境 | 采用的接口 | 效果 |
| --- | --- | --- |
| DSH 0.1.5+ | `stat(id)` + `open(id, 'read')` → `handle.read().events` | 完整回放 |
| DSH 0.1.0-rc.6 | `readStoredRevision(id)` + `readRaw(id)`（JSONL） | 完整回放（旧路径保留） |
| 两者都不存在 | — | 退回内存账本（不崩溃，历史费用缺失） |

- **只改服务端 `lib/index.js` 的日志读取路径**：价格表、前端界面、HTTP 接口均未变动，因此不涉及价格或界面行为变化。
- 接口签名已对照 DSH 0.1.5-rc.2 核对：`open(id, access, options?)`、`stat(id, options?)`、`SessionHandleReadResult.events` 与本实现一致。

### 校验

- `node --check` 全量通过（`lib/index.js`、`lib/client.js`、`lib/pricing.js`、`scripts/install.js`）。
- 价格回归测试 **8/8 通过**：覆盖周末全天闲时、工作日峰/谷、`deepseek-flash` 与旧名同价、`deepseek-v4-pro` 沿用 08-23 旧价、以及 2026-09-10 12:00 调价前后分界。

---

## [0.2.1] — 2026-09-13

### 价格更新：内置 2026-09-10 的 V4.1-Flash 调价政策

官方于 2026-09-10 12:00（北京时间）起调整 **flash 系列**定价，本次更新把这条政策追加进价格引擎。

**Flash 系列单价变化**（单位：元 / 每百万 tokens）：

| 计费项 | 闲时（旧 → 新） | 高峰（旧 → 新） |
| --- | --- | --- |
| 输入（缓存命中） | 0.05 → **0.02**（↓60%） | 0.1 → **0.04** |
| 输入（缓存未命中） | 1.5 → **1**（↓33%） | 3 → **2** |
| 输出 | 4.5 → **4**（↓11%） | 9 → **8** |

- 高峰仍为空闲时段的 **2 倍**：工作日 09:00-12:00、14:00-18:00；其余时间闲时；周末全天闲时（2026-08-23 起）。
- 美元价同步调整为 0.006 / 0.3 / 1.2（高峰）与 0.003 / 0.15 / 0.6（闲时）。

### 新增模型名 `deepseek-flash`

- V4.1-Flash 的正式模型名是 `deepseek-flash`；旧名 `deepseek-v4-flash`、`deepseek-v4-flash-vision-exp` 由 V4.1-Flash 提供服务、按 Flash 单价计费。
- 两个名字都已内置，无需任何手动配置。

### `deepseek-v4-pro` 价格保持不变

- 官方已**撤回**原定 2026-09-14 的下线 / 路由计划，V4 Pro 继续提供、计费方式不变。
- 引擎按“新政策未点名该模型 → 自动沿用上一版政策”的规则取价，因此 V4 Pro 仍是 2026-08-23 的旧价（闲时 0.15 / 4.5 / 13.5 元，高峰翻倍），无需额外条目。

### 影响范围

- **只动价格表**，不改界面与接口。
- **历史消息按当时的政策计价**：这次更新不会让老会话的既有费用发生变化，只有 `since`（2026-09-10 12:00）之后的消息按新价计算。
- 已安装用户：更新插件后重启 Web 应用（`dsh web`）并刷新页面即生效。

### 校验

- `node --check` 全量通过（`lib/index.js`、`lib/client.js`、`lib/pricing.js`、`scripts/install.js`）。
- 价格数据策展自官方公告与 [DeepSeek 官方价格页](https://api-docs.deepseek.com/zh-cn/quick_start/pricing/)。

---

## [0.2.0] — 2026-08-25

首个公开版本。

- 会话标题栏「计费」按钮：实时余额 + 当前会话费用，可展开按轮次的请求明细（含子代理会话）。
- 每条 assistant 消息下方的当轮费用徽标。
- 官方计价引擎：峰谷定价、周末全天闲时、缓存命中 / 未命中差异化计价、推理 token 按输出价计费。
- 历史政策时间表：模型改名、降价、下架均按当时价格计算。
- 会话日志回放：重启前产生的费用也会被重新计价，不漏账。
